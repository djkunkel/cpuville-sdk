# Build artifacts
*.o
*.bin
*.cim
*.rom
*.hex
*.cmd
*.cas
*.rel
core
*~
\#*

# # Vendored tool build outputs (binaries built into bin/ by build.sh)
# /bin/zmac
# /bin/z80asm

# zxcc runtime asset: the vestigial CP/M BIOS loaded at startup.
# Installed by vendor/build-zxcc.sh; must be tracked despite *.bin above.
!/bin/bios.bin
!/vendor/zxcc-0.5.7/Z80/bios.bin

# Vendored source build intermediates
/vendor/zmac/src/zmac
/vendor/zmac/src/zmac.c
/vendor/zmac/src/doc
/vendor/zmac/src/doc.inl
/vendor/z80asm/src/z80asm
/vendor/tio/build
/vendor/zxcc-0.5.7/build

# Vendored source trees fetched by vendor/clone.sh (not committed)
/vendor/z88dk/

# Stray scratch file at repo root
/a.bin
